import { SafeInsertionPipe } from './safe-insertion.pipe';

describe('SafeInsertionPipe', () => {
  it('create an instance', () => {
    const pipe = new SafeInsertionPipe();
    expect(pipe).toBeTruthy();
  });
});
